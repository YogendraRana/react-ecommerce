export interface ItemType {
    id: number;
    name: string;
    category: string;
    categoryId: number;
    price: number;
    brand: string;
    description: string;
    image: string;
}

export interface StoreMenuType {
    id: number;
    label: string;
    icon: string;
}